<%layout('./layouts/boilerplate')%>

    <div class="row">
        <h1 class="text-center">Edit <%=campground.title%> Campground</h1>
        <div class="col-6 offset-3">
            <form action="/campgrounds/<%=campground._id%>?_method=PUT" method="POST">
                <div class="mb-3">
                    <label class="form-label" for="tile">Title</label>
                    <input class="form-control" type="text" name="campground[title]" id="tile"
                        value="<%=campground.title%>">
                </div>
                <div class="mb-3">
                    <label class="form-label" for="location">Location</label>
                    <input class="form-control" type="text" name="campground[location]" id="location"
                        value="<%=campground.location%>">
                </div>
                <div class="mb-3">
                    <label class="form-label" for="price">Price</label>
                    <div class="input-group">
                        <span class="input-group-text" id="basic-addon1">$</span>
                        <input class="form-control" type="number" name="campground[price]" id="price"
                            placeholder="0.00">
                    </div>
                    <div class="mb-3">
                        <label class="form-label" for="image">Update Image</label>
                        <input class="form-control" type="text" name="campground[image]" id="image"
                            value="<%=campground.image%>">
                        <div class="mb-3">
                            <label class="form-label" for="current-image">Current Image</label>
                            <img src="<%=campground.image%>" alt="" id="current-image">
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label" for="description">Change description</label>
                        <textarea class="form-control" name="campground[description]" id="description" cols="60" rows="10"><%=campground.description%></textarea>
                    </div>
                    <div class="mb-3">
                        <button class="btn btn-info">Update Campground</button>
                    </div>
            </form>
            <a class="btn btn-danger" href="/campgrounds/<%=campground._id%>">Cancel</a>
        </div>
    </div>